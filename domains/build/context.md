# Build domain

Owner: `product-manager` into `software-architect` into `tech-lead` into `code-writer`.
Side projects and product work that is not the job hunt.

## Standing shape

Dhruv works alone, on Windows, with Claude Code and no spare API keys. Any design assuming
a team, a budget, or a hosted service with a paid key is the wrong design — say so early
rather than three steps in.

## Projects

- `../jopply` — job pipeline. Python, requests, no framework. Active.
- Others live as siblings under `D:\Coding\Projects\`.

Add a project here when the crew starts working on it, with one line on its stack and one
line on what it is for. Not before.
